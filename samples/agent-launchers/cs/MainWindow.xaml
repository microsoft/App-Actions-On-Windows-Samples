<Window x:Class="SampleAgentLauncher.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Sample Agent Launcher"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="20,20,20,10"
                   HorizontalAlignment="Center"/>

        <!-- Agent Invocation Info Panel -->
        <Border x:Name="InvocationInfoPanel"
                Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="15"
                Margin="20,0,20,10"
                Visibility="Collapsed">
            <StackPanel Spacing="8">
                <TextBlock Text="Agent Invocation Received"
                           FontSize="16"
                           FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Action:"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="InvocationActionText"
                               Text=""
                               IsTextSelectionEnabled="True"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Agent Name:"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="InvocationAgentNameText"
                               Text=""
                               IsTextSelectionEnabled="True"/>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="Prompt:"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="InvocationPromptText"
                               Text=""
                               TextWrapping="Wrap"
                               IsTextSelectionEnabled="True"/>
                </StackPanel>
                <StackPanel Spacing="4">
                    <TextBlock Text="Full URI:"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="InvocationUriText"
                               Text=""
                               TextWrapping="Wrap"
                               FontFamily="Cascadia Code"
                               FontSize="11"
                               IsTextSelectionEnabled="True"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Vertical"
                    Spacing="15"
                    HorizontalAlignment="Left"
                    Margin="20">
            <StackPanel Orientation="Horizontal"
                        Spacing="15"
                        HorizontalAlignment="Left">
                <TextBlock x:Name="StaticRegistrationStatusText"
                           Text="static-sample-agent-launcher: Checking..."
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Spacing="15"
                        HorizontalAlignment="Left">
                <TextBlock x:Name="DynamicRegistrationStatusText"
                           Text="sample-agent-launcher: Checking..."
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"/>
                <Button x:Name="ToggleDynamicLauncherButton"
                        Content="..."
                        Click="ToggleDynamicLauncherButton_Click"
                        Padding="20,8"
                        IsEnabled="False"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical" Spacing="15" Margin="20">
            <TextBlock x:Name="StatusTextBlock"
                       Text=""
                       Margin="20,10"
                       HorizontalAlignment="Center"
                       FontWeight="SemiBold"
                       Foreground="Red"
                       Visibility="Collapsed"/>

            <Expander Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="Registered Agent Launchers" Margin="0" Padding="0">
                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Grid x:Name="LaunchersTableGrid" Margin="8" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Header Row -->
                        <Border Grid.Row="0" Grid.Column="0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,4">
                            <TextBlock Text="Name" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </Border>
                        <Border Grid.Row="0" Grid.Column="1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,4">
                            <TextBlock Text="Display Name" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </Border>
                        <Border Grid.Row="0" Grid.Column="2" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,4">
                            <TextBlock Text="Action ID" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </Border>

                        <!-- Data will be populated dynamically -->
                        <StackPanel x:Name="LaunchersDataPanel" Grid.Row="1" Grid.ColumnSpan="3" Margin="0,8,0,0">
                            <TextBlock Text="Loading..." FontStyle="Italic" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Padding="8"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Expander>
        </StackPanel>

        <Grid Grid.Row="5" Margin="20,0,20,20" VerticalAlignment="Stretch">
            <Expander IsExpanded="False" Background="{ThemeResource SubtleFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" HorizontalAlignment="Stretch" 
                      HorizontalContentAlignment="Stretch" 
                      VerticalAlignment="Bottom"
                      Header="Debug Output"
                      Margin="0"
                      Padding="0">
                <ScrollViewer x:Name="OutputScrollViewer"
                              Height="300"
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="OutputTextBlock"
                               Text="Application output will appear here..."
                               TextWrapping="Wrap"
                               Margin="8"
                               HorizontalAlignment="Stretch"
                               HorizontalTextAlignment="Left"
                               FontFamily="Cascadia Code"
                               FontSize="12"
                               IsTextSelectionEnabled="True"/>
                </ScrollViewer>
            </Expander>
        </Grid>
    </Grid>
</Window>
